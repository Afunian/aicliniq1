function QuoteStrip() {
  return (
    <section className="quote-strip section" aria-label="Brand quote">
      <div className="container">
        <blockquote>AI should reduce friction, not add complexity.</blockquote>
        <p className="quote-subline">
          AICliniq is focused on practical systems that make communication and
          follow-up easier to manage.
        </p>
      </div>
    </section>
  );
}

export default QuoteStrip;
